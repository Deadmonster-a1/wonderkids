# WonderKids Academy — Pre-Deployment Audit Report

**Date:** 2026-07-20
**Stack (detected, not assumed):** Vite 6 + React 19 + TypeScript SPA (client-side routing via react-router-dom, no SSR), Tailwind CSS v4, Express 4 API in `server/` with Prisma + PostgreSQL (Supabase), zod, bcrypt, JWT, Helmet, express-rate-limit, nodemailer. **Not Next.js** — the audit prompt assumed Next.js; every check below was adapted to the real stack (e.g. security headers via `public/_headers` instead of `next.config.js`; SEO via a custom `useSeo` hook instead of `next/head`).
**Repo state:** no git repository exists anywhere in this tree, so every fix below is a plain uncommitted file change. There is also no production domain yet — all absolute URLs (canonical, sitemap, OG, JSON-LD) use `https://example.com` as an explicit placeholder that **must be replaced before launch**.

Legend: ✅ Pass · 🔧 Auto-fixed · ⚠️ Needs your decision

---

## Section 2 — Security

| # | Check | Status | Reason |
|---|-------|--------|--------|
| 1 | No secrets committed anywhere in repo (incl. git history) | ✅ | No git repo exists in this tree at all, so there is no history to leak into. `.env` (real secrets) was never in scope of any commit. |
| 2 | `.env*` gitignored; only `.env.example` with placeholders committed | ✅ | `.gitignore` already covers `.env*`; `.env.example` already contains only placeholder values — nothing to change. |
| 3 | `npm audit` run, high/critical vulnerabilities listed | 🔧/⚠️ | Root project: **0 vulnerabilities**. `server/`: **1 high** — `nodemailer` (pinned `^6.9.16`) has multiple advisories (SMTP command/CRLF injection, TLS validation, SSRF via raw message option); fix requires `npm audit fix --force` which bumps to `nodemailer@9.0.3`, a breaking major-version jump. Not applied automatically (new/upgraded dependency = out of safe scope per your own checklist) — **you need to upgrade and re-test the email flow** (`server/src/utils/email.ts`). |
| 4 | No exposed `.git/`, `.env`, config, or backup files under `public/` | ✅ | Confirmed nothing sensitive lives under the (newly created) `public/` dir; Vite only ever serves `public/*` and the built `dist/`. |
| 5 | Security headers present (`X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, `Strict-Transport-Security`, CSP) | 🔧 | Created [`public/_headers`](public/_headers) (Netlify-convention file, copied to `dist/` root on build) with all four headers plus a CSP scoped to what the site actually loads (self, Google Fonts, `images.unsplash.com`, same-origin `/api`). **If you deploy anywhere other than Netlify, these need porting to that host's header mechanism** (Vercel `vercel.json`, nginx, etc.) — see note below. |
| 6 | Admission/enquiry form has server-side validation | ✅ | Already real: every write route in `server/src/routes/{public,admin}.ts` runs a zod schema via `validate()` middleware before the controller runs. Nothing to fix. |
| 7 | Form inputs escaped/sanitized before render/storage (XSS) | 🔧 | Found and fixed a related gap not explicitly on your checklist: `server/src/utils/csv.ts`'s admin CSV export did not neutralize leading `=`/`+`/`-`/`@` in user-supplied fields — classic CSV/Formula Injection when an admin opens an export in Excel. Fixed by prefixing risky leading characters. HTML emails already run through a local `escapeHtml()` — no gap there. No `dangerouslySetInnerHTML` anywhere in `src/`. |
| 8 | API routes have rate limiting/abuse protection | ✅ | Already real: `helmet()` + `express-rate-limit` (200/15min global on `/api`, 10/15min on `/api/admin/login`) in `server/src/index.ts`. Not touched — tuning limits further is a judgment call, not a gap. |
| 9 | HTTPS enforced; no mixed-content | 🔧 | Added `Strict-Transport-Security` (see #5) and `upgrade-insecure-requests` in the CSP. No hardcoded `http://` asset URLs found anywhere in `src/`. Actual TLS termination/redirect is a hosting-config concern once a real domain + host is chosen. |
| 10 | No hardcoded credentials/internal URLs in client bundles | ✅ | No `VITE_`-prefixed env vars exist, so nothing in `.env` can leak into `import.meta.env` in the client bundle. `@google/genai` is an unused dependency (never imported in `src/` or `server/src/`) — confirmed no key exposure risk from it. |

**⚠️ Not on your checklist but found and worth your immediate attention:** the real `server/.env` (not `.env.example`) contains **live secrets** — a real Supabase DB password, JWT secret, and Gmail SMTP app password — plus `ADMIN_PASSWORD=admin123` (a trivially guessable password) paired with an admin email that is your real address. This file is correctly gitignored and was never committed, so no code fix applies, but **you should rotate the SMTP app password and set a strong `ADMIN_PASSWORD` before going live**, since anyone with local file access (or a future accidental commit) would have full admin + mailbox access. Not reproduced here for obvious reasons.

**⚠️ Also found:** `server/src/middleware/upload.ts` allows `image/svg+xml` uploads served statically from `/uploads`. SVGs can carry inline `<script>` — a stored-XSS vector if an admin account is ever compromised and someone opens the uploaded file URL directly. The upload route is already behind `authMiddleware` (admin-only), so severity is low, but flagging rather than silently narrowing the file-type allowlist, since that could break legitimate SVG icon uploads (your call).

---

## Section 3 — SEO

| # | Check | Status | Reason |
|---|-------|--------|--------|
| 1 | Unique title (50–60 chars) + meta description (~150–160 chars) per page | 🔧 | Added [`src/hooks/useSeo.ts`](src/hooks/useSeo.ts) (dependency-free — no react-helmet, per "no new deps") and wired it into all 7 public pages with unique titles/descriptions derived from each page's own existing copy. `index.html`'s static fallback tags updated to match the homepage (for non-JS crawlers, which can't run the hook). |
| 2 | Open Graph + Twitter Card tags | 🔧 | `useSeo.ts` sets `og:title/description/url/type/site_name` and `twitter:card/title/description` per route; static fallback also added to `index.html`. **Caveat:** this is a client-rendered SPA with no SSR/prerendering, so social-link scrapers that don't execute JS (most of them) will only ever see the static homepage OG tags, not page-specific ones, for deep links. Fixing that fully would mean adding prerendering/SSR — an architecture change, out of scope here. |
| 3 | Canonical URL on every page | 🔧 | Set per-route via `useSeo.ts`, plus a static default in `index.html`. Uses `https://example.com` placeholder — **replace with your real domain before launch.** |
| 4 | `robots.txt` exists, doesn't block the whole site | 🔧 | Created [`public/robots.txt`](public/robots.txt) — allows all public routes, disallows `/admin`, points to the sitemap. |
| 5 | `sitemap.xml` exists, lists real pages | 🔧 | Created [`public/sitemap.xml`](public/sitemap.xml) listing the 7 public routes (admin excluded). Uses the same placeholder domain. |
| 6 | One `<h1>` per page, logical nesting, no skipped levels | 🔧 | Every page already had exactly one `<h1>` ✅. Fixed real skips (verified live in-browser on all 7 routes after the fix, not just by reading source): `AboutUsPage.tsx` (h1→h3 and h2→h4), `Features.tsx`, `AcademicsPage.tsx`, `AdmissionsPage.tsx`, `TeachersPage.tsx` (all h2/h1→h4), and — found only during live verification, not the initial file read — `Footer.tsx`'s three column headings were `h4` and skipped a level on **every page** since Footer sits after whatever heading level a page ends on. All fixed by renumbering the heading tag only; `index.css` gives h1–h6 identical font styling, so there is zero visual change. `AnnouncementBanner.tsx` and `NotificationToast.tsx` used headings (`h3`/`h4`) inside transient, dismissible toast/popup notifications that can appear before or interleaved with the page's real heading order — demoted both to `<p>` with `role="status"`, which is the standard pattern for transient live-region content (it shouldn't be in the document outline at all). |
| 7 | Every meaningful image has `alt` text | ✅ | All real, rendered `<img>` tags already have descriptive alt text. Nothing to fix. |
| 8 | Internal links use descriptive text | ✅ | No "click here"-style links found anywhere. |
| 9 | Favicon + web app manifest present | 🔧 | Created a placeholder SVG favicon ([`public/favicon.svg`](public/favicon.svg) — a simple brand-indigo "W" monogram, since no real logo asset exists in the repo; Header/Footer currently just use an emoji + text wordmark) and [`public/site.webmanifest`](public/site.webmanifest), both linked from `index.html`. **Swap the favicon for real branding before launch.** |
| 10 | Structured data (JSON-LD) | 🔧 | Added a sitewide `EducationalOrganization` JSON-LD block, injected once from `App.tsx` via `useOrganizationJsonLd()` in `useSeo.ts`. Populated only from the same fallback values `Footer.tsx` already uses (school name, email, phone, address) — nothing invented. |

**⚠️ Flagged, not fixed:** `Footer.tsx` has 4 placeholder `href="#"` links (Careers, Mandatory Disclosure, **Privacy Policy**, Terms of Service) pointing to pages that don't exist in this repo. Your checklist scopes "broken internal links" fixes to pages that *already exist* — these don't, so they're out of safe-fix scope. The missing **Privacy Policy** page is worth prioritizing: the Admissions and Contact forms collect a child's and parent's personal information with nowhere on-site explaining how it's used (see the compliance flag under Section 6 in your original prompt).

---

## Section 4 — Performance

| # | Check | Status | Reason |
|---|-------|--------|--------|
| 1 | Lighthouse audit run, scores recorded | ✅ | Ran via CLI against a production build (`vite build` + `vite preview`). **Performance: 33 · Accessibility: 95 · Best Practices: 96 · SEO: 100.** Measured with Lighthouse's default simulated-mobile throttling (4x CPU slowdown, throttled 3G) in a sandboxed dev VM — treat the Performance number as directionally real but likely pessimistic vs. a real CDN-hosted deployment on real hardware; the SEO/Accessibility/Best-Practices scores are throttling-independent and more directly trustworthy. See the diagnosis below the table. |
| 2 | Images modern format + lazy-loaded below fold | 🔧 | The two real hero images (`Features.tsx`) already request `?...&auto=format` from Unsplash, which content-negotiates WebP/AVIF automatically — already effectively passing. Added the missing `loading="lazy"` to `TeachersPage.tsx` (teacher avatars) and `AnnouncementBanner.tsx` (announcement image); `Gallery.tsx` images already had it. |
| 3 | `font-display: swap` + critical fonts preloaded | 🔧 | Found a real bug: `index.html` linked **5** font families (Nunito, Inter, Fredoka, Plus Jakarta Sans, Quicksand) while `src/index.css` separately re-imported Inter + Outfit — and the actual CSS theme only ever uses Inter + Outfit. The site was downloading 5 families to use 2. Fixed `index.html` to request only Inter + Outfit (still `display=swap`, still preconnected) and removed the duplicate render-blocking `@import` from `index.css`. No visual change — same two fonts render as before. |
| 4 | No unused/oversized JS bloating initial load | 🔧 | Every page statically imported the full three.js/@react-three/fiber/drei stack via `Scene.tsx` (all pages) and `KidsToysScene.tsx` (Home only) — confirmed zero code-splitting existed anywhere. Code-split both behind `React.lazy` + `Suspense` in `Hero.tsx` and `PageHero3D.tsx`. Build output confirms it worked: `KidsToysScene` alone is a **1000 KB / 275 KB gzipped** chunk now loaded asynchronously and only on the homepage, instead of blocking every page's initial script. Verified visually afterward (all 7 routes) that the 3D scenes still render correctly with no layout shift. |
| 5 | Static assets have proper caching headers | 🔧 | Added to the same `public/_headers` file: `Cache-Control: public, max-age=31536000, immutable` for `/assets/*` (safe — Vite content-hashes those filenames) and `max-age=0, must-revalidate` for `index.html` (so users always get the latest build's asset references). |

**Performance diagnosis:** even after code-splitting, Lighthouse's own trace shows `KidsToysScene`'s chunk alone consumes **~8.7 s of main-thread time** under 4x CPU throttling — it's not just large to download, it's expensive to *execute* (a lot of Three.js geometry/material setup for the homepage hero). Code-splitting was the correct and safe mechanical fix (it got this cost off the critical rendering path and off every non-homepage route entirely), but reducing the 3D scene's actual complexity is a creative/product decision outside this audit's scope — flagging it here as the single biggest lever left for real-world performance.

---

## Section 5 — Accessibility

| # | Check | Status | Reason |
|---|-------|--------|--------|
| 1 | Color contrast meets WCAG AA (4.5:1 body text) | ⚠️ | **Cannot be reliably assessed** — see the design-token finding below. `brand-slate` (`#64748B`, the de facto muted-text color sitewide) is borderline (~4.6:1 on white); several spots layer opacity on top (`text-white/50`, `text-brand-slate/60`, `text-brand-slate/80`) which likely drop below 4.5:1. Flagging rather than guessing at a fix, since color is explicitly a design decision per your own checklist. |
| 2 | Every interactive element keyboard-reachable/operable | 🔧 | `Gallery.tsx`'s masonry grid items and lightbox backdrop were `<div onClick>` with no keyboard path. Added `role="button" tabIndex={0}` + Enter/Space handling to grid items, an `Escape`/arrow-key handler to the lightbox, and `aria-label`s to its close/prev/next buttons. All other interactive elements sitewide (nav, hamburger, accordions, filter tabs) were already real `<button>`/`<a>` elements — already keyboard-operable. |
| 3 | Form inputs have associated `<label>` elements | 🔧 | `ContactPage.tsx` was already correct. `AdmissionsPage.tsx`'s 6 fields (student name, parent name, email, phone, grade, message) had `<label>`s with no `htmlFor` and inputs with no `id` — added matching pairs to all 6, verified live in-browser. |
| 4 | Focus states visible, not suppressed | 🔧 | A sitewide `*:focus-visible` ring already exists in `index.css`, but several elements cancelled it with nothing to replace it: removed `outline-none` from `AcademicsPage.tsx`'s accordion trigger and `FAQ.tsx`'s question buttons; removed `focus:ring-0 outline-none` from all 6 `AdmissionsPage.tsx` inputs and `outline-none` from all 3 `ContactPage.tsx` inputs (letting the existing global ring apply in both cases); added `focus:ring-2 focus:ring-brand-indigo` to 4 `SettingsAdmin.tsx` social-link inputs that were missing it while sibling inputs in the same form already had it. |
| 5 | Animations respect `prefers-reduced-motion` | 🔧 | Previously handled in exactly one hook (`useScrollAnimation.ts`) and nowhere else. Added a sitewide CSS override in `index.css` (near-zero animation/transition duration under the media query) and skipped Lenis smooth-scroll initialization entirely in `useSmoothScroll.ts` when reduced motion is requested, mirroring the existing hook's own pattern. Not rewritten: the many individual Framer Motion/GSAP animation calls throughout the codebase still run their JS-driven transforms — the CSS override neutralizes their visible duration, but a truly complete fix would mean touching every `motion.*` component's transition props individually, which is beyond a mechanical audit fix. |
| 6 | ARIA labels on icon-only buttons/links | 🔧 | Added `aria-label`s to: `Gallery.tsx` lightbox close/prev/next buttons, `AnnouncementBanner.tsx` prev/next chevrons (its close button already had one), `NotificationToast.tsx`'s dismiss button, and `Footer.tsx`'s 4 social icons (Facebook/Instagram/Twitter/YouTube — previously icon-only with no accessible name at all). Note: your checklist's own example mentions a "WhatsApp float button" — `FloatingElements.tsx` contains a well-labeled one already, but that component is dead code, never rendered on any actual route. Left as-is rather than silently wiring an unused component into the live layout (that would be a layout change, out of scope). |

**⚠️ The single biggest accessibility finding, flagged not fixed:** dozens of components (11+ files) use Tailwind classes like `bg-primary`, `bg-secondary`, `bg-tertiary`, `text-on-surface`, `text-on-surface-variant`, `bg-surface`, `font-body-md`, `font-display-lg`, `font-headline-sm`, `font-label-caps` — **none of these tokens are defined anywhere.** `src/index.css`'s `@theme` block only defines `brand-navy/indigo/violet/coral/sky/slate/light` colors and `font-display`/`font-body`; no `tailwind.config.*` exists to define the rest another way. Under Tailwind v4's CSS-first theme system, referencing an undefined token silently generates **no CSS at all** for that utility — meaning real rendered colors/fonts across large parts of the site (including the root layout `<div>` in `App.tsx` itself, via `bg-surface font-body-md text-on-surface`) are not what the code implies, and contrast/legibility can't be fully verified until this is resolved. This is unambiguously a design-system decision (defining or renaming an entire color/type-scale token system), not a mechanical fix, so it was flagged and left untouched everywhere it appears.

---

## Files changed

**Created:** `public/_headers`, `public/robots.txt`, `public/sitemap.xml`, `public/favicon.svg`, `public/site.webmanifest`, `src/hooks/useSeo.ts`, `AUDIT_REPORT.md`
**Modified:** `index.html`, `src/index.css`, `src/App.tsx`, `src/hooks/useSmoothScroll.ts`, `src/components/{Hero,PageHero3D,Features,FAQ,Gallery,Footer,AnnouncementBanner,NotificationToast}.tsx`, `src/pages/{Home,AcademicsPage,ProgramsPage,AboutUsPage,TeachersPage,AdmissionsPage,ContactPage}.tsx`, `src/pages/admin/SettingsAdmin.tsx`, `server/src/utils/csv.ts`

No git repository exists in this tree, so there is nothing to diff via `git status` — the list above is exhaustive; review the files directly.

Two files were also created **outside** the project folder for local preview tooling only (not part of the audited codebase): `P:\wonderkids-main\.claude\launch.json` (lets the dev server preview correctly — the repo's own `npm run dev` collides with a `PORT=5173` environment variable already set in this sandbox, unrelated to any code here) and a scratch `lighthouse-report.json` generated during the Performance check (deleted after extracting the scores above).

## Summary of ⚠️ items needing your decision

1. **Rotate secrets** in `server/.env` before launch — weak `ADMIN_PASSWORD`, plus the SMTP app password and DB password are live values in a local file.
2. **Upgrade `nodemailer`** (`server/`) — 1 high-severity advisory, fix requires a breaking major-version bump; needs testing against the email flow.
3. **Buy/point a real domain** and replace every `https://example.com` placeholder (canonical tags, sitemap, OG/JSON-LD, `robots.txt`).
4. **Confirm final hosting provider** — `public/_headers` is Netlify-syntax; port to `vercel.json`/nginx/other if deploying elsewhere. Also confirm whether the API (`server/`) will share the frontend's origin in production — if not, `connect-src`/`img-src` in the CSP and `CORS_ORIGIN` server-side both need updating together.
5. **Write a Privacy Policy page** — forms collect a child's personal data; the footer link is currently a placeholder pointing nowhere.
6. **Decide on the undefined Tailwind design tokens** (`bg-primary`, `text-on-surface`, `font-display-lg`, etc., 11+ files) — currently silently render as no styling at all under Tailwind v4.
7. **SVG upload allowlist** in `server/src/middleware/upload.ts` — low-severity stored-XSS surface if an admin account is ever compromised.
8. Consider simplifying the homepage's `KidsToysScene` 3D component — it alone accounts for most of the Performance score gap.
